# Find Radar v5 — keyless map

Changed the Lost & Found basemap from CARTO to OpenStreetMap raster tiles.

- No API key is required.
- The large CARTO "API KEY REQUIRED" watermark is gone.
- OpenStreetMap attribution remains visible as required.
- The existing dark/sleek styling is preserved via MapLibre raster paint + the app vignette.
- Private-location marker, color picker, reports, photos, and radar splash are unchanged.
